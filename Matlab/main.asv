clc
projfunctions

% https://uk.mathworks.com/help/symbolic/solve-a-system-of-differential-equations.html
syms x(t) y(t)
L = [1 2; -1 1];
w = [x; y];
system = [L, w]
odeSystem = diff(system(2)) == system(1)*system(2);
dsolve(odeSystem);


expm(L*t)